{
  "name": "vantage-mosaic",
  "version": "0.1.0",
  "private": true,
  "description": "Fleet-wide MCP UI design system — Zod-validated, taxonomy-organized, registry-gated, streaming-ready components.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "size-limit": "turbo run size-limit",
    "registry-drift": "node packages/mosaic/scripts/mosaic-registry-drift-check.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "turbo": "2.5.4",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
