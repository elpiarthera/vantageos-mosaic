/**
 * Shared error messages for the FormProvider runtime layer.
 */
export const FORM_PROVIDER_CONTEXT_MISSING =
  "useMosaicFormContext must be used inside <FormProvider>. Wrap your tree with <FormProvider form={...}> from @vantageos/mosaic/{react,preact}/forms.";
